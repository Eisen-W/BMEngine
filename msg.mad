int tile = 16;

DIALOGUES {
    auto {
        id = 1;
        once = true;
        rect = [(0 * tile, 0 * tile, 20 * tile, 15 * tile), (16,16,16,16)];
        speaker = "Narrator";
        lines = ["welcome idiot"];
    }

    interact {
        id = 2;
        once = false;
        rect = (100, 200, 32, 48);
        speaker = "Old man";
        lines = ["its dangerous to go alone, take this"];
    }
}